# Core (needed for the synthetic smoke test + baselines) -- runs anywhere
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10
pyyaml>=6.0
tqdm>=4.65

# Deep models (needed on the GPU box / Cursor for the temporal GNN + DANN)
# Install torch matching your CUDA, then PyG. See CURSOR_PROMPT.md.
torch>=2.1
torch-geometric>=2.4

# Plots / dev
matplotlib>=3.7
pytest>=7.4
